<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.black_survivor_dictinary">

    <application
        android:allowBackup="true"
        android:icon="@drawable/black"
        android:label="@string/app_name"
        android:roundIcon="@drawable/black"
        android:supportsRtl="true"
        android:theme="@style/Theme.Black_survivor_dictinary">
        <meta-data android:name="android.max_aspect" android:value="2.1" />
        <activity android:name=".Item"></activity>
        <activity android:name=".Character_mini" />
        <activity android:name=".Character" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>